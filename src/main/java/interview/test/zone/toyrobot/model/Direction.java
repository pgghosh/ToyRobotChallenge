package interview.test.zone.toyrobot.model;

/**
 * The order of the Direction enum constants is important as it helps in a neat solution for left and right commands implementation.
 */
public enum Direction {
    SOUTH, WEST, NORTH, EAST
}
